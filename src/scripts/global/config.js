const IMAGE_SIZE = {
  SMALL: 'small',
  MEDIUM: 'medium',
  LARGE: 'large'
}

const CONFIG = {
  BASE_URL: 'https://restaurant-api.dicoding.dev',
  DEFAULT_IMAGE_SIZE: IMAGE_SIZE.SMALL
}

export default CONFIG

export { IMAGE_SIZE }
